// arducam pin = arduino pin

// pin 1 is left-most pin when lens is facing you
const int CS  = 7; // arducam pin 1
      // MOSI = MOSI (may be labelled COPI)
      // MISO = MISO (may be labelled CIPO)
      // SCLK = SCK
      // GND  = GND (rail)
      // +5V  = Vcc (rail--3.3V may be OK for 2MP arducam)
      // SDA  = SDA 
      // SCL  = SCL 